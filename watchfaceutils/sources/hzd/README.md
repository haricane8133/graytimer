1-bit conversions (400px) of the machine renders from the Horizon Wiki
(https://horizon.fandom.com, files `<Machine>-Render.png`): alpha channel with the ground
shadow removed gives the outline, and the render's own shading thresholded (no dithering)
gives the interior detail. Used by `generate_geometric.py` for the Horizon Zero Dawn faces.
